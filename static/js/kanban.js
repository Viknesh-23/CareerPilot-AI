(() => {
  let dragged; const token=document.querySelector('meta[name="csrf-token"]')?.content;
  document.querySelectorAll('.kanban-card').forEach(card=>{card.addEventListener('dragstart',()=>{dragged=card;card.classList.add('dragging')});card.addEventListener('dragend',()=>card.classList.remove('dragging'));});
  document.querySelectorAll('.kanban-cards').forEach(zone=>{zone.addEventListener('dragover',e=>{e.preventDefault();zone.classList.add('dragover')});zone.addEventListener('dragleave',()=>zone.classList.remove('dragover'));zone.addEventListener('drop',async e=>{e.preventDefault();zone.classList.remove('dragover');if(!dragged)return;const column=zone.closest('.kanban-column');const status=column.dataset.status;if(status===dragged.closest('.kanban-column').dataset.status)return;const body=new URLSearchParams({status,csrf_token:token});const response=await fetch(`/applications/${dragged.dataset.id}/status`,{method:'POST',headers:{'Accept':'application/json','Content-Type':'application/x-www-form-urlencoded'},body});if(response.ok){zone.appendChild(dragged);document.querySelectorAll('.kanban-column').forEach(col=>{col.querySelector('.badge').textContent=col.querySelectorAll('.kanban-card').length;});}else{alert('Could not update application status. Please refresh and try again.');}});});
})();
